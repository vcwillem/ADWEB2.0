import {HousekeepingBookModel} from './housekeeping-book.model';

describe('HousekeepingBookModel', () => {
  it('should create an instance', () => {
    expect(new HousekeepingBookModel()).toBeTruthy();
  });
});
